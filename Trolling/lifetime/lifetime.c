#include <stdio.h>
#include <unistd.h>

int     main()
{
        int     day, month, year;
        printf("GREETINGS.\nThis is program will calculate how long you have been alive.\n");
        printf("Enter your birthday (DD/MM/YYYY) to begin.\n");
        while (1)
        {
                if (scanf("%2d/%2d/%4d", &day, &month, &year) == 3);
                {
                        if (day >= 1 && day <= 31 && month >= 1 && month <= 12 && year > 0 && year <= FIND HOW TO FIND CURRENT DATE ON UR SYSTEM)
                }
        }

}